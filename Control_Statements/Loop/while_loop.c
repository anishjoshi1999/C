#include <stdio.h>
int main()
{
    int count = 0;
    printf("\nStart of the while loop");
    while (count < 3)
    {
        printf("\nHello World");
    }
    printf("\nEnd of the while loop");
    return 0;
}