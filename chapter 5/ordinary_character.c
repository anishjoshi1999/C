#include <stdio.h>
int main()
{
    int day, month, year;
    printf("Enter day,month and year in DD-MM-YYYY format: ");
    scanf("%d-%d-%d", &day, &month, &year);
    printf("Day: %d\n", day);
    printf("Month: %d\n", day);
    printf("Year: %d\n", day);
    return 0;
}